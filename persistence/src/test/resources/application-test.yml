logging:
  level:
    root: INFO
    org.springframework.jdbc.core: TRACE

spring:
  datasource:
    username: test
    password: test
  jpa:
    properties.hibernate:
      batch_size: 50
      fetch_size: 50
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
